# Pin exact versions on every install — prevents silent semver drift
save-exact=true

# Always run audit on install so vulnerabilities surface immediately
audit=true

# Require the lockfile to match package.json; fail if they diverge
# Use `npm ci` (not `npm install`) in CI/CD for fully reproducible builds
package-lock=true
